# 参与开发

感谢参与读日和。请先阅读 [架构文档](docs/ARCHITECTURE.md)。

1. Fork 仓库，创建自己的开发分支。
2. `npm ci` 安装依赖，`npm run build` 生成扩展。
3. 在 Chrome / Edge 开发者模式加载 `dist/`。
4. 修改后运行与改动相关的测试；核心变更请运行 `npm test` 和 `npm run test:e2e`。
5. 提交 Pull Request，说明问题、行为变化、验证方式及已知限制。

新增翻译接口应使用明确的服务地址和最小权限，不抓取私有令牌，不将整页或收藏自动上传。请勿提交 API 密钥、个人单词本、浏览器配置、node_modules 或 dist。

报告注音问题时，提供最短可复现日文、实际读音、预期读音，以及是否跨越 HTML 标签。报告网页兼容问题时注明浏览器版本和公开可访问的示例地址；请删除私人页面内容。
